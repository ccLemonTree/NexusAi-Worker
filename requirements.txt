# ── Kafka ──────────────────────────────────────────────────────────────────
aiokafka==0.11.0
# aiokafka 0.11 uses cramjam for LZ4/snappy/zstd — lz4 package alone is NOT enough
cramjam==2.11.0

# ── HTTP / async I/O ───────────────────────────────────────────────────────
aiohttp==3.12.13
aiofiles==23.2.1
requests==2.32.3
httpx==0.27.0

# ── Computer vision ────────────────────────────────────────────────────────
# headless variant: no GUI libs needed in a container
opencv-python-headless==4.13.0.92
numpy==2.2.4
Pillow==10.4.0

# ── Triton inference client (gRPC) ─────────────────────────────────────────
# grpcio / grpcio-status are transitive deps of tritonclient and pymilvus;
# let pip resolve a mutually compatible version rather than pinning them here.
tritonclient[grpc]==2.66.0

# ── Data validation / config ───────────────────────────────────────────────
pydantic==2.11.7
pydantic-settings==2.14.1
python-dotenv==1.2.2
PyYAML==6.0.1

# ── Vector DB ──────────────────────────────────────────────────────────────
pymilvus==3.0.0

# ── Object storage (EOS / S3-compatible) ───────────────────────────────────
boto3==1.38.33
