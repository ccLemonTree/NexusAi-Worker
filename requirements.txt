# ── Kafka ──────────────────────────────────────────────────────────────────
aiokafka==0.11.0
lz4                  # LZ4 codec support for aiokafka (broker may compress with LZ4)

# ── HTTP / async I/O ───────────────────────────────────────────────────────
aiohttp==3.12.13
aiofiles==23.2.1
requests==2.32.3
httpx==0.27.0

# ── Computer vision ────────────────────────────────────────────────────────
# headless variant: no GUI libs needed in a container
opencv-python-headless==4.13.0.92
numpy==2.2.4
Pillow==10.4.0

# ── Triton inference client (gRPC) ─────────────────────────────────────────
# grpcio / grpcio-status are transitive deps of tritonclient and pymilvus;
# let pip resolve a mutually compatible version rather than pinning them here.
tritonclient[grpc]==2.66.0

# ── Data validation / config ───────────────────────────────────────────────
pydantic==2.11.7
pydantic-settings==2.14.1
python-dotenv==1.2.2
PyYAML==6.0.1

# ── Vector DB ──────────────────────────────────────────────────────────────
pymilvus==3.0.0
