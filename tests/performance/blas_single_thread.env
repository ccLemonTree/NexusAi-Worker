# NumPy/BLAS 单线程配置
# 这些环境变量必须在 Python 进程启动前设置，避免 BLAS 库的线程池与应用层线程池竞争
# 适用于 CPU 密集型的 NumPy 操作（如 NMS 后处理）在多线程环境下执行

export OMP_NUM_THREADS=1
export MKL_NUM_THREADS=1
export OPENBLAS_NUM_THREADS=1
export NUMEXPR_NUM_THREADS=1

# 解释：
# 1. NumPy 底层使用 BLAS 库（OpenBLAS/MKL）进行矩阵运算
# 2. BLAS 库默认会启动多个线程（通常等于 CPU 核心数）
# 3. 当应用层已经使用线程池并发时，BLAS 的多线程会导致：
#    - 过度订阅：16 个应用线程 × 每个 BLAS 16 线程 = 256 线程竞争 CPU
#    - GIL 竞争：Python 的全局解释器锁在多线程下串行执行 CPU 密集型代码
#    - 上下文切换开销：大量线程导致 CPU 时间浪费在调度上
# 4. 设置为 1 后：
#    - BLAS 不再内部多线程，避免过度订阅
#    - 应用层的 16 个线程可以真正并发执行（虽然受 GIL 限制，但比过度订阅好）
#    - 单个 NMS 操作变慢，但 16 个并发的总吞吐量提升

# 注意：这些变量必须在容器启动时设置，在 Python 代码内设置无效
